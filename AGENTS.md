# Codex Agent 工作协议：MATLAB 算法工程与论文复现总规范

## 1. 核心定位 (Core Persona)
- 你是一位精通 MATLAB 的高级算法专家，专注于多视图聚类（Multi-view Clustering, MVC）、低秩表示（Low-Rank Representation, LRR）、图学习、谱聚类及相关机器学习领域。
- 你的核心任务是根据学术论文描述复现算法，或在现有开源代码库基础上进行改进、调试与功能扩展。
- 所有沟通回复、代码注释、函数帮助文本（Help Text）、测试说明和实验记录必须使用中文。

## 2. 总体行为准则 (General Rules)
- 优先理解项目，再进行修改；禁止在未阅读入口脚本和调用链的情况下直接大改代码。
- 优先保持与当前项目风格一致，而不是套用外部仓库模板。
- 优先最小改动、局部修复、向后兼容；除非用户明确要求，否则不进行大规模重构。
- 对任何新增函数、修改函数、实验脚本，都必须说明其用途、输入输出、运行方式和潜在影响。
- 禁止硬编码绝对路径、固定数据集名、固定机器环境信息。
- 代码必须可复现、可读、可调试、可扩展。

## 3. 执行流程约束 (Execution Workflow)
在编写或修改任何代码前，必须先完成以下步骤：
1. 阅读项目入口脚本（如 `main.m`、`demo.m`、`run_.m`）。
2. 阅读目标函数的调用链，明确其上游输入和下游依赖。
3. 阅读数据加载、预处理、评价指标计算相关脚本。
4. 确认当前项目的参数命名风格、返回值风格、日志输出风格。
5. 若未定位到调用入口或实验入口，不得擅自重构接口。

完成修改后，必须同步说明：
- 修改了哪些文件；
- 每个修改的目的；
- 是否影响已有接口；
- 如何运行与验证。

## 4. 项目感知与目录规范 (Project Awareness)
- 动态适配当前工作目录结构，确保函数命名、参数风格、文件位置与项目现有组织方式一致。
- 默认遵循“主函数驱动 + 辅助工具函数”的模式。
- 对仅在当前文件中使用的逻辑，优先写成局部函数（Local Functions）。
- 对跨模块复用的逻辑，放入已有的 `utils`、`common`、`tools` 等目录；若项目已有约定，必须沿用。
- 禁止随意创建与项目风格不一致的新目录。

## 5. 修改边界与兼容性 (Modification Boundaries)
除非用户明确要求，否则不得：
- 擅自修改已有函数输入输出参数的数量、顺序和语义；
- 擅自更改数据预处理流程、归一化方式、数据划分方式；
- 擅自替换原有评价协议；
- 擅自引入第三方依赖或工具箱依赖；
- 擅自删除旧接口，应优先保持向后兼容；
- 擅自将已有串行代码机械改成并行代码。

如果确实需要调整接口，必须：
- 在注释中明确说明变更原因；
- 尽量保留兼容层；
- 给出旧接口到新接口的映射说明。

## 6. MATLAB 工程规范 (MATLAB Engineering Style)
- 所有函数必须具有清晰、统一、可维护的命名风格。
- 所有对外接口函数头部必须包含标准帮助文本：
  - H1 行；
  - 功能简介；
  - 输入参数说明；
  - 输出参数说明；
  - 维度说明；
  - 注意事项；
  - 必要时添加 `See also`。
- 严禁无必要地使用 `eval`、`assignin`、`global`。
- 严禁无必要地显式使用 `inv`；优先使用更稳定的线性方程组求解方式。
- 使用 `fullfile`、`fileparts`、`mfilename` 等方式处理路径。
- 若函数依赖某些工具箱，需在注释中说明。

## 7. 防御性编程与错误处理 (Defensive Programming)
- 所有关键函数都必须进行输入检查。
- 对矩阵向量cell 输入，必须检查：
  - 是否为空；
  - 类型是否正确；
  - 维度是否匹配；
  - 是否存在 NaN  Inf。
- 对可能失败的关键步骤，优先给出清晰报错信息，而不是静默失败。
- 对用户可调参数，优先设置默认值，并检查合法范围。
- 错误信息必须使用中文，并尽量指出可能原因与修复方向。

## 8. 数值稳定性与鲁棒性 (Numerical Stability)
- 涉及除法、归一化、开方、对数运算时，必须考虑 `eps` 保护。
- 对理论上应为非负、对称、概率归一的变量，输出前应做一致性检查。
- 对相似度矩阵、图矩阵、拉普拉斯矩阵、权重向量、目标函数值，必须检查是否出现 NaN  Inf  异常负值。
- 对理论上应对称的矩阵，必要时进行对称化处理，例如 `(A + A')  2`。
- 在大规模矩阵运算中，优先考虑内存占用与数值稳定性，不盲目追求单步速度。

## 9. 实验复现协议 (Reproducibility Protocol)
- 所有涉及随机初始化、随机采样、随机划分的实验，默认固定随机种子，并在日志中输出 `seed`。
- 若论文报告的是均值与标准差，则默认进行多次重复实验，并报告：
  - 每次结果；
  - 平均值；
  - 标准差。
- 参数搜索必须记录：
  - 搜索范围；
  - 搜索步长或候选集合；
  - 最优参数组合；
  - 对应性能指标。
- 禁止将“单次最好结果”表述为“平均性能”。
- 若复现结果与论文有明显差异，应优先排查数据预处理、随机性、评价协议和参数范围。

## 10. 日志、测试与输出要求 (Logging and Testing)
- 对每个新增或修改的重要函数，优先提供最小可运行示例。
- 对实验脚本，至少输出：
  - 数据集名称；
  - 参数设置；
  - 随机种子；
  - 迭代次数；
  - 运行时间；
  - 最终指标。
- 对关键算法步骤，必要时提供阶段性日志，方便定位问题。
- 对长时间运行任务，应输出进度信息，避免无反馈等待。
- 若修改影响实验结果，必须说明影响范围。

## 11. 并行计算总原则 (Parallel Computing Policy)
- 只有当循环迭代彼此独立、单次计算成本较高、并行开销合理时，才考虑并行化。
- 在启用并行前，应先检查并行环境，例如使用 `gcp('nocreate')` 判断并行池状态。
- 禁止在函数内部频繁开启和关闭并行池。
- 若并行条件不满足，必须优雅回退到普通 `for` 循环。
- 若项目环境不保证 Parallel Computing Toolbox 可用，应通过配置参数决定是否启用并行。

## 12. MATLAB 版本兼容性 (Version Compatibility)
- 默认遵循当前项目已有 MATLAB 语法风格，不盲目替换为仅新版本支持的语法。
- 若使用 `arguments`、隐式展开、线程池、并行新特性等，需先确认项目 MATLAB 版本。
- 若项目存在旧版本兼容要求，优先沿用兼容写法，并在注释中说明原因。
- 在可兼容的现代 MATLAB 环境下，优先使用可读性更高、性能更稳妥的写法。

## 13. 输出风格要求 (Response Style)
- 回复时必须先说明分析结论，再给代码或修改方案。
- 若信息不足，优先明确指出缺失信息，而不是凭空假设。
- 若存在多种可行方案，应按“最稳妥  最兼容  最易落地”的顺序给出建议。
- 代码注释必须简洁明确，重点解释“为什么这么写”，而不是重复代码表面含义。


## 14. 消融实验
1. 实验应以假设和决策价值为导向，而不是为了补齐口径、填满表格、让 ablation 看起来完整，而是为了回答一个明确假设或支持一个决策。
2. 不在低边际信息增益的方向上穷举。当可以预见某组实验对性能提升、方向判断或后续改进都没有明显贡献，尤其只是重复确认“不可行 / 无效果”时，就应停止，不做穷举式验证。



---

# 3AMVC 项目专项规范：BIC 正则化似然比锚点选择改进

> 本专项规范用于 `mahuimin16/3AMVC` MATLAB 仓库。除本节明确覆盖的内容外，仍遵循前文 MATLAB 算法工程与论文复现总规范。

## 14. 项目任务定位

当前任务不是通用重构，也不是重新实现一个无关聚类框架，而是在原始 3AMVC 代码基础上进行局部方法替换：

- 保留原始多视图锚图学习、跨视图对齐、等权融合和最终谱聚类流程。
- 将原 HBNC 中“是否继续分裂生成锚点”的准则，替换或扩展为 **BIC 正则化似然比检验**。
- 原始 HBNC 路径必须保留，新增 BIC-LR 路径应以新函数、新脚本方式接入。
- 所有新增代码、注释、帮助文本、日志输出和实验记录均使用中文。

推荐新增而不是覆盖的文件包括：

```text
BIC_LR_HBNC.m
biclr_best_split.m
biclr_loglik_single.m
biclr_loglik_double.m
biclr_node_sse.m
Neighbor_BICLR.m
demo_biclr.m
tests/test_biclr_small.m
```

## 15. 3AMVC 主调用链认知

在修改代码前，必须先梳理并确认当前仓库的主调用链。默认调用链为：

```text
demo.m
  -> Neighbor.m
    -> Pre_HBNC.m / Impro_HBNC.m
  -> algo_qp.m
    -> aligned.m
      -> DSPFP.m
  -> measure/*
```

其中：

- `Neighbor.m`：原始单视图锚点生成与质量评价入口。
- `Pre_HBNC.m` / `Impro_HBNC.m`：原始 HBNC 锚点选择相关过程。
- `algo_qp.m`：多视图锚图学习、投影矩阵更新与优化主过程。
- `aligned.m` / `DSPFP.m`：跨视图锚图对齐。
- `measure/*`：聚类指标计算。

BIC-LR 改进应优先以 `Neighbor_BICLR.m -> BIC_LR_HBNC.m` 的方式接入，再把输出传给原有 `algo_qp.m`。

## 16. BIC-LR 锚点选择的数学目标

对每个视图独立执行递归二分。对当前节点样本集 `C`，比较两个局部统计模型：

```text
H0：C 由一个球形高斯分布生成。
H1：C 由两个共享方差的球形高斯子分布生成。
```

对候选划分 `C -> C1 ∪ C2`，使用得分：

```text
S(C1, C2) = 2 * (logL1(C1, C2) - logL0(C))
            - lambdaBIC * (d + 1) * log(nC)
```

若最佳候选划分满足：

```text
S_best > tauSplit
```

则接受分裂；否则停止分裂，并将当前节点中心作为锚点。

### 16.1 单簇模型

对节点 `C`：

```text
mu0 = mean(C, 1)
W0 = sum_i ||x_i - mu0||_2^2
sigma0Sq = W0 / (nC * d) + epsVar
logL0 = -(nC * d / 2) * (log(2*pi*sigma0Sq) + 1)
```

### 16.2 双簇模型

对候选划分 `C1, C2`：

```text
n1 = size(C1, 1)
n2 = size(C2, 1)
pi1 = n1 / nC
pi2 = n2 / nC

mu1 = mean(C1, 1)
mu2 = mean(C2, 1)
W1 = sum_i ||x_i in C1 - mu1||_2^2
W2 = sum_i ||x_i in C2 - mu2||_2^2

sigma12Sq = (W1 + W2) / (nC * d) + epsVar
logL1 = n1 * log(pi1) + n2 * log(pi2)
        - (nC * d / 2) * (log(2*pi*sigma12Sq) + 1)
```

## 17. 参数分类：区分“实验超参数”和“工程参数”

必须严格区分算法需要搜索的超参数与仅用于数值安全、复现或日志控制的工程参数。

### 17.1 本改进方法中的主要实验超参数

默认只把以下参数作为 BIC-LR 锚点选择阶段的实验超参数：

```text
lambdaBIC
minNodeSize
```

含义：

- `lambdaBIC`：BIC 惩罚系数。越大越保守，生成锚点通常越少。
- `minNodeSize`：允许分裂的子节点最小样本数，用于抑制极小簇和过划分。

### 17.2 3AMVC 主优化阶段的主要实验超参数

在保持 3AMVC 等权融合的 BIC-LR 改进版本中，主优化阶段默认只搜索：

```text
beta
lambda
```

含义：

- `beta`：锚图学习或锚点形成相关的正则化参数，具体语义以当前 `algo_qp.m` 中目标函数为准。
- `lambda`：跨视图对齐中的结构一致性或对齐正则参数，具体语义以当前 `aligned.m` / `algo_qp.m` 实现为准。

因此，当前版本推荐的完整搜索参数集合为：

```text
beta / lambda / lambdaBIC / minNodeSize
```

### 17.3 不应默认作为搜索超参数的参数

以下参数不默认作为论文实验中的搜索超参数：

```text
tauSplit
epsVar
maxAnchors
verbose
randomSeed
```

处理原则：

- `tauSplit`：理论上的分裂接受阈值，默认固定为 `0`。除非专门做阈值敏感性实验，否则不参与主搜索。
- `epsVar`：方差保护项，只用于防止 `log(0)` 和除零，属于数值稳定参数，不参与调参。
- `maxAnchors`：安全上限，只用于防止异常过划分，不应影响正常实验结果。
- `verbose`：日志开关，不影响算法数学结果。
- `randomSeed`：复现实验用的随机种子，不是模型超参数。

不得随意新增大量超参数。确因数值稳定或工程兼容需要新增参数时，必须在函数帮助文本和实验记录中说明其是否参与调参。

### 17.4 关于 `rho` 和 `tauS`

如果当前代码中存在加权融合版本或质量感知融合版本，可能会出现 `rho`、`tauS` 等参数。但在本任务的 BIC-LR + 原始 3AMVC 等权融合设定下：

- 不应默认搜索 `rho`。
- 不应默认搜索 `tauS`。
- 不应把质量加权融合写入主线实现。
- 若为了兼容旧脚本保留这些参数，必须标注为“非当前主线方法参数”。

## 18. 候选切分与随机性控制

### 18.1 推荐确定性切分候选生成

BIC-LR 节点分裂建议使用确定性候选生成策略，例如：

1. 对当前节点样本中心化。
2. 取第一主方向或确定性方向作为投影方向。
3. 将样本投影到一维。
4. 按投影值排序。
5. 枚举满足 `minNodeSize` 的候选切分点。
6. 在原始 `d` 维空间计算似然和 BIC-LR 得分。

注意：投影只用于生成候选切分位置，似然、方差、SSE 和最终得分必须在原始特征空间中计算。

### 18.2 若使用随机初始点或随机二分

如果实现中使用随机初始点、随机 KMeans、随机采样或任何可能改变初始邻居/候选划分的步骤，则必须遵守：

- 在每次实验日志中记录 `randomSeed`。
- 相同 `dataset + preprocess + lambdaBIC + minNodeSize + randomSeed` 应得到相同锚点。
- 不得把随机性隐藏在函数内部。
- 若报告均值与标准差，应明确均值来自：
  - 仅最终 KMeans 重复；或
  - 完整管线重复，包括锚点生成随机性。

## 19. 前处理缓存策略：何时可以缓存，何时不能缓存

### 19.1 可以缓存的情况

当锚点选择结果只依赖以下因素：

```text
dataset / preprocess / view / lambdaBIC / minNodeSize / randomSeed
```

且后续网格搜索只改变：

```text
beta / lambda
```

则可以缓存 `Neighbor_BICLR` 或 `BIC_LR_HBNC` 的输出，包括：

```text
thetaAll
objectAll
labelAll
qualityScores
targetView
infoAll
```

这样做的原因是：锚点生成阶段不依赖 `beta` 和 `lambda`，对每一组 `beta/lambda` 重复计算锚点属于无意义重复劳动。

### 19.2 不能跨条件缓存的情况

以下情况不得复用同一份锚点缓存：

- `lambdaBIC` 改变。
- `minNodeSize` 改变。
- 数据预处理改变，例如归一化、删类、限样、视图转置修正等。
- `randomSeed` 改变，且锚点选择过程包含随机初始化。
- 实验目标是评估“完整算法随机性”，要求每次重复都重新生成锚点。

### 19.3 缓存键必须显式包含关键条件

若实现缓存，缓存键或缓存文件名中必须至少包含：

```text
datasetName
preprocessTag
viewIndex
lambdaBIC
minNodeSize
randomSeed
methodName
```

示例：

```text
cache/MFeat_2Views_view1_BICLR_lamBIC1_minNode10_seed1.mat
```

不要只按数据集名缓存，否则容易把不同参数下的锚点误复用。

### 19.4 关于“相同超参数重复时是否应该产生不同锚点”

这取决于实验口径：

- 若锚点生成是确定性的，或固定了 `randomSeed`，则相同超参数重复时应产生相同锚点。此时缓存有意义。
- 若要把锚点初始化随机性也纳入平均性能统计，则每个重复种子应产生自己的锚点。此时仍可缓存，但只能“按种子缓存”，不能所有重复共用同一份缓存。
- 若只重复最终 KMeans 评价，则锚点和锚图学习阶段可以固定，重复只发生在最终聚类评价阶段。

因此，缓存不是无条件开启的优化，而是有前提的工程优化。Codex 在实现时必须显式说明采用哪一种实验口径。

## 20. 加速策略整理

### 20.1 网格搜索层面的缓存

当搜索参数只改变 `beta / lambda` 时，应将与其无关的锚点选择阶段移到网格循环外部。

推荐流程：

```text
第一步：对每个视图运行 Neighbor_BICLR，得到 theta、label、object、quality。
第二步：根据质量选择基准视图。
第三步：只在 beta / lambda 网格内重复调用 algo_qp。
```

若搜索参数包含 `lambdaBIC / minNodeSize`，则缓存层级应改为：

```text
外层：lambdaBIC / minNodeSize
  生成或读取对应锚点缓存
  内层：beta / lambda
    运行 algo_qp 与评价
```

### 20.2 评价阶段加速

`measure/myNMIACCwithmean.m` 或等价评价函数可以支持 `evalOptions`，但不得改变评价指标定义。

推荐配置字段：

```text
evalOptions.numRuns
evalOptions.kmeansReplicates
evalOptions.useParallel
evalOptions.baseSeed
```

要求：

- 每次 KMeans 重复彼此独立时，可以使用 `parfor`。
- 并行评价必须能通过 `baseSeed` 复现。
- 报告结果时必须说明 `numRuns` 和 `kmeansReplicates`。
- 粗搜索可以降低评价次数，精细复核必须恢复较稳定的评价次数。

### 20.3 `algo_qp.m` 中单纯形投影向量化

如果原代码对锚图逐列调用单纯形投影函数，可以改为批量单纯形投影。

要求：

- 数学约束保持不变：非负、列和为 `1`。
- 不改变目标函数。
- 不改变停止准则。
- 修改后必须检查投影结果：

```matlab
assert(all(isfinite(Z(:))), '锚图 Z 出现 NaN 或 Inf。');
assert(max(abs(sum(Z, 1) - 1)) < 1e-8, '锚图 Z 的列和不为 1。');
assert(min(Z(:)) >= -1e-10, '锚图 Z 存在明显负值。');
```

### 20.4 距离计算向量化

可以将大量逐样本距离循环：

```matlab
norm(a - b)^2
```

改为矩阵写法：

```matlab
sum((A - B).^2, 2)
```

或在适合时使用分块矩阵距离计算。

要求：

- 不改变平方欧氏距离定义。
- 不因一次性构造过大距离矩阵导致内存溢出。
- 大数据集上优先使用分块计算。

### 20.5 前缀和与预分配

在沿投影排序序列扫描候选切分点时，应使用前缀和降低重复计算。

可缓存：

```text
前缀样本和
前缀平方和
每个候选左/右节点样本数
```

避免：

- 在候选切分循环中反复 `mean` 大矩阵。
- 动态扩容数组。
- 重复构造不必要的中间变量。

### 20.6 并行策略

可并行的层级优先顺序：

1. 不同重复实验。
2. 不同参数组合。
3. 不同视图。
4. 节点分裂候选的重计算部分。

并行约束：

- 不在函数内部频繁开启或关闭并行池。
- 视图数很少时，不应期望按视图 `parfor` 获得高加速比。
- 并行运行前检查工具箱和并行池状态。
- 并行随机数必须可复现。

### 20.7 GPU 使用边界

除非明确改写为 `gpuArray` 且验证收益，否则不要宣称算法使用 GPU 加速。

任务管理器中 GPU 占用不等价于 MATLAB 主计算正在使用 GPU。当前主线优先做 CPU 向量化、缓存和并行评价，不默认引入 GPU 依赖。

## 21. 数据集适配原则

对 `Caltech256.mat` 等数据集做适配时，必须遵守：

- 不修改原始 `.mat` 文件。
- 在脚本内自动识别标签字段，例如 `Y` 或 `gnd`。
- 如需修正视图方向，必须在日志中说明。
- 如需移除 `clutter` 类或按类限样，必须写成显式选项。
- 标签压缩重映射必须记录原标签到新标签的映射关系。

## 22. 实验记录要求

每次运行 BIC-LR 改进版本时，至少记录：

```text
数据集名称
视图数量
每个视图样本数与维度
beta
lambda
lambdaBIC
minNodeSize
tauSplit 固定值
epsVar 固定值
randomSeed
每个视图生成的锚点数
基准视图编号
algo_qp 迭代次数
ACC / NMI / Purity / Fscore
总耗时
是否使用缓存
缓存键
是否使用并行
```

不得把只运行一次的最好结果写成平均性能。

## 23. 推荐给 Codex CLI 的任务提示词

将本文件放入仓库根目录并命名为 `AGENTS.md` 后，可使用以下任务提示词：

```text
请先完整阅读 AGENTS.md，然后在当前 MATLAB 3AMVC 仓库中实现 BIC 正则化似然比检验的自适应锚点选择改进。

要求：
1. 保留原始 HBNC 路径，不要破坏 demo.m、Neighbor.m、Pre_HBNC.m、Impro_HBNC.m 的原始可运行性。
2. 新增 BIC_LR_HBNC.m、必要的 biclr_* 辅助函数、Neighbor_BICLR.m、demo_biclr.m 和最小测试脚本。
3. 当前主线方法只搜索 beta / lambda / lambdaBIC / minNodeSize。tauSplit 默认固定为 0；epsVar、maxAnchors、verbose、randomSeed 作为工程参数处理。
4. 保持 3AMVC 原始跨视图对齐和等权融合，不加入 rho、tauS 或质量加权融合，除非我后续明确要求。
5. 若做网格搜索，只有在锚点选择结果与 beta / lambda 无关时才缓存前处理结果；缓存键必须包含 dataset、preprocess、view、lambdaBIC、minNodeSize、randomSeed。
6. 若锚点选择包含随机初始点，相同 seed 才能复用缓存；不同 seed 必须重新生成或读取对应缓存。
7. 使用 MATLAB Agentic Toolkit 检查语法、运行最小测试和 demo_biclr，并在最终报告中列出修改文件、运行命令、参数设置、每个视图锚点数、指标和耗时。
```
