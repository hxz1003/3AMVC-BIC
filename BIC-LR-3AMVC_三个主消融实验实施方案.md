# BIC-LR-3AMVC 三个主消融实验实施方案

## 一、文档目的

本文档只服务于当前 `BIC-LR-3AMVC + 原 3AMVC 等权融合` 主线，不涉及代码实现，不涉及 MATLAB 运行，也不包含 `.m` 脚本。

当前只做三个主方向的消融实验设计：

1. `w/o BIC`：验证 BIC 复杂度惩罚是否必要。
2. `w/o LR`：验证似然比建模是否必要。
3. `w/o alignment`：验证跨视图对齐是否必要。

本文档默认保留两个参考方法：

- `M1 Original 3AMVC`：原始 `Neighbor / Pre_HBNC / Impro_HBNC`，原对齐，原等权融合。
- `M2 BIC-LR-3AMVC`：完整 BIC-LR，自适应锚点选择，原对齐，原等权融合。

因此，每个主消融实验都建议采用“三方法对照”口径：

- 实验 A：`M1` vs `M2` vs `M3 w/o BIC`
- 实验 B：`M1` vs `M2` vs `M4 w/o LR`
- 实验 C：`M1` vs `M2` vs `M7 w/o alignment`

这里的重点不是补齐所有可能的变体，而是回答三个最关键的问题：

1. 完整 BIC-LR 的收益是否来自 BIC 惩罚。
2. 完整 BIC-LR 的收益是否来自似然比建模本身。
3. 即使锚点生成改进了，原 3AMVC 的跨视图对齐是否仍然不可省。

---

## 二、统一实验口径

### 2.1 数据集与预处理

所有主消融实验必须在完全相同的数据条件下进行，建议沿用当前仓库主线数据集：

- `MFeat_2Views`
- `Reuters-1200`
- `Wikifea`
- `Caltech101-all`

统一要求：

- 使用同一份数据加载流程。
- 使用同一份标签压缩与重映射逻辑。
- 是否移除 `clutter` 类必须全方法一致。
- 是否按类限样必须全方法一致。
- 视图方向修正必须全方法一致。
- 不允许某个方法单独改归一化方式。

### 2.2 随机性与重复口径

本轮主表推荐采用：

- 锚点生成固定；
- 主优化固定；
- 只重复最终 KMeans 评价。

这样做的理由是：

- 三个主消融的目标是比较模块必要性，而不是比较完整随机性传播。
- 锚点生成阶段一旦固定，方法差异更容易归因到对应模块本身。

补充稳定性分析时，再单独做“完整管线按 `seed` 重复”的附表。

### 2.3 评价指标与选优规则

当前仓库已有的核心指标应全部保留：

- `ACC`
- `NMI`
- `Purity`
- `Fscore`
- `Precision`
- `Recall`
- `AR`
- `Entropy`

附加记录：

- `Runtime_total`
- `Runtime_anchor`
- `Runtime_align`
- 每个视图锚点数 `m_v`
- 基准视图编号 `targetView`
- 每个视图锚点质量分数 `q_v`
- 是否出现过小节点
- 是否触发 `maxAnchors`
- 是否出现异常锚点

选优规则必须统一为：

1. 参数筛选和最终方法展示都以 `best ACC` 为唯一主排序标准。
2. 这里的 `best ACC` 指重复评价后的平均 `ACC`，不是单次最优 `ACC`。
3. 其余指标必须全部保存，并在“该 best ACC 参数组合”下同步报告。
4. 如果出现多个参数组合的平均 `ACC` 完全相同，则全部保留到精评阶段继续评估。
5. 最终表格可以只展示其中一个代表组合，但脚注中应注明存在 `ACC` 并列最优组合。

换言之：

- “选择标准”只看 `ACC`。
- “结果报告”必须同时保留仓库已有全部指标。

### 2.4 两阶段执行方式

所有三个主消融都建议分两阶段执行。

第一阶段：粗搜索

- 目的：快速找到每个方法在每个数据集上的候选高 `ACC` 区域。
- KMeans 重复次数较少。
- 只保留若干个 `ACC` 最优候选组合。

第二阶段：精评

- 目的：对粗搜索筛出的候选最优组合做稳定复核。
- 增加 KMeans 重复次数。
- 用平均 `ACC` 确定最终 best 配置。
- 其余指标一并汇报。

---

## 三、实验 A：w/o BIC

### 3.1 实验目的

该实验只回答一个问题：

> 在已经使用似然比建模的前提下，BIC 复杂度惩罚是否是必要的？

如果去掉 BIC 后：

- 锚点数量明显膨胀；
- 运行时间明显上升；
- `ACC` 或 `NMI` 不稳定甚至下降；

则可以说明 BIC 惩罚不仅是“理论修饰项”，而且在工程上确实起到了抑制过划分、稳定锚点结构的作用。

### 3.2 对照方法

本实验采用三方法对照：

- `M1 Original 3AMVC`
- `M2 BIC-LR-3AMVC`
- `M3 w/o BIC`

其中 `M3 w/o BIC` 的定义为：

- 保留 BIC-LR 的递归二分框架。
- 保留单簇/双簇共享方差球形高斯建模。
- 保留投影排序扫描候选切分点。
- 保留 `minNodeSize`。
- 保留原 3AMVC 的基准视图选择、跨视图对齐和等权融合。
- 仅删除 BIC 惩罚项。

即分裂得分从：

- `2(logL1-logL0) - lambdaBIC*(d+1)*log(nC)`

退化为：

- `2(logL1-logL0)`

### 3.3 变量控制

该实验必须控制以下变量一致：

- 数据集与预处理一致。
- `beta` 搜索范围一致。
- `lambda` 搜索范围一致。
- `minNodeSize` 搜索范围一致。
- `randomSeed` 一致。
- KMeans 重复次数一致。
- `tauSplit=0` 固定。
- `epsVar`、`maxAnchors`、`verbose` 固定。

其中：

- `M2` 搜索 `beta / lambda / lambdaBIC / minNodeSize`
- `M3` 搜索 `beta / lambda / minNodeSize`

因为 `M3` 中 `lambdaBIC` 已经失效，应明确记为：

- `lambdaBIC = N/A`

而不是假装它仍然是有效参数。

### 3.4 推荐执行步骤

#### 阶段一：粗搜索

1. 先固定数据集、预处理、评价协议和 `randomSeed`。
2. 对 `M2` 执行完整粗网格搜索，得到一批高 `ACC` 的参数组合。
3. 对 `M3` 使用相同的 `beta / lambda / minNodeSize` 网格执行粗搜索。
4. `M1` 作为参考基线，只搜索其已有有效参数，不强行补出 `lambdaBIC / minNodeSize`。
5. 每个方法都保留该数据集上平均 `ACC` 最高的若干组合进入精评。

#### 阶段二：精评

1. 对粗搜索筛出的 `Top-K ACC` 组合增加 KMeans 重复次数。
2. 对每组参数记录平均 `ACC`、标准差和全部附属指标。
3. 最终只按平均 `ACC` 选择每个方法的 best 结果。
4. 在 best `ACC` 对应结果下，一并抄出：
   - `NMI`
   - `Purity`
   - `Fscore`
   - `Precision`
   - `Recall`
   - `AR`
   - `Entropy`
   - `Runtime`
   - `AnchorNums`
   - `BaselineView`

### 3.5 本实验重点观察什么

重点不是只看 `ACC` 高低，还要同步看：

- 每个视图锚点数是否明显增多。
- 总锚点数是否显著高于 `M2`。
- 是否更容易接近 `maxAnchors`。
- `q_v` 是否变差。
- `Runtime_anchor` 是否显著增大。
- `ACC` 标准差是否变大。

理想的结论形式应是：

- 若 `M3` 的 `ACC` 不升反降，且锚点数明显增多，则可支持“BIC 惩罚是必要的”。
- 若 `M3` 与 `M2` 的 `ACC` 接近，但 `M3` 锚点更多、耗时更高，则仍可支持“BIC 惩罚有工程价值”。

### 3.6 结果记录重点

本实验建议额外单列：

- `DeltaAnchors = MeanAnchors(M3) - MeanAnchors(M2)`
- `DeltaRuntime = Runtime(M3) - Runtime(M2)`

这样能更直接证明：

- 去掉 BIC 到底是在“提升有效表达”，还是只是在“生成更多锚点”。

---

## 四、实验 B：w/o LR

### 4.1 实验目的

该实验要回答的问题是：

> 在已经保留递归二分框架、候选切分扫描和最小节点约束的情况下，似然比建模是否是必要的？

也就是说，本实验不是比较“有无锚点自适应”，而是比较：

- 用统计模型做分裂判定；
- 还是只用非似然的几何型准则做分裂判定。

### 4.2 为什么不直接套原 `criterion.m`

根据当前仓库原始 3AMVC 代码：

- `criterion.m` 接收的是“目标样本到同一候选范围内样本的距离”；
- 它本质上是“目标邻域阈值判据”，不是“对某个候选切分位置打分”的函数；
- 它与当前 BIC-LR 的“投影排序后逐候选切分点评分”结构并不一一对应。

因此，若当前要做一个与 `M2` 可对照的 `w/o LR` 主消融，最稳妥的方案不是直接套 `criterion.m`，而是：

- 保留当前递归二分骨架；
- 保留投影扫描；
- 保留 `minNodeSize`；
- 仅把分裂得分从“似然比”替换成“非似然的 SSE 改善量”。

这比直接回退到整套原 HBNC 更能隔离“是否使用 LR”这一因素。

### 4.3 对照方法

本实验采用三方法对照：

- `M1 Original 3AMVC`
- `M2 BIC-LR-3AMVC`
- `M4 w/o LR`

其中 `M4 w/o LR` 定义为：

- 保留递归二分框架。
- 保留第一主方向投影与排序扫描候选切分点。
- 保留 `minNodeSize`。
- 保留基准视图选择、跨视图对齐和等权融合。
- 不使用单簇/双簇高斯似然。
- 不计算 `logL0`、`logL1`。
- 使用 `DeltaSSE = W0 - (W1 + W2)` 作为候选切分得分。

接受分裂的条件建议设为：

- `best DeltaSSE > 0`

同时仍保留：

- `minNodeSize`
- `maxAnchors`

作为必要的结构安全约束。

### 4.4 变量控制

该实验中必须固定：

- 数据集与预处理一致。
- `beta` 搜索范围一致。
- `lambda` 搜索范围一致。
- `minNodeSize` 搜索范围一致。
- `randomSeed` 一致。
- KMeans 重复次数一致。
- `tauSplit=0` 固定，但在 `M4` 中不再承担主判据作用。

由于 `M4` 中已经没有 LR，也就没有原始意义上的 BIC 惩罚，因此：

- `lambdaBIC = N/A`

应明确标注为无效参数，不纳入搜索。

### 4.5 推荐执行步骤

#### 阶段一：粗搜索

1. 先固定数据集和预处理。
2. 对 `M2` 执行标准粗搜索，获得参考 `ACC` 区间。
3. 对 `M4` 在同一 `beta / lambda / minNodeSize` 网格上执行粗搜索。
4. `M1` 按原有口径跑出其 best `ACC` 结果。
5. 每个方法各自保留若干个 best `ACC` 候选组合进入精评。

#### 阶段二：精评

1. 提高 KMeans 重复次数。
2. 对每个候选组合记录平均 `ACC` 和标准差。
3. 最终以平均 `ACC` 选出 `M4` 的 best 结果。
4. 在 best `ACC` 对应结果下同步报告全部现有指标。

### 4.6 本实验重点观察什么

本实验主要关注以下现象：

- `M4` 的锚点数是否明显偏多。
- `M4` 的 `ACC` 是否低于 `M2`。
- `M4` 的 `q_v` 是否恶化。
- `M4` 是否更容易产生形状不合理的锚点划分。
- `M4` 的结果是否对 `minNodeSize` 更敏感。

因为 `DeltaSSE > 0` 的分裂倾向通常比 LR 判据更宽松，所以本实验尤其要关注：

- 是否出现过多但无效的锚点；
- 是否出现 `ACC` 看似不低，但 `NMI / Fscore / q_v` 更差的情况。

### 4.7 如何解释可能结果

推荐的解释逻辑如下：

- 若 `M4` 的 best `ACC` 明显低于 `M2`，则可直接支持“LR 建模必要”。
- 若 `M4` 的 `ACC` 接近 `M2`，但锚点数显著更多、`Runtime_anchor` 更高，则可支持“LR 建模更高效、更稳定”。
- 若 `M4` 在某些数据集上 `ACC` 接近甚至略高于 `M2`，仍要检查：
  - 这种提升是否来自极端锚点数；
  - 这种提升是否伴随更差的 `NMI / Purity / Fscore`；
  - 这种提升是否具有稳定性。

换言之，本实验不能只盯着单一 `ACC` 数字看，还必须看：

- 结构代价；
- 运行代价；
- 指标一致性。

---

## 五、实验 C：w/o alignment

### 5.1 实验目的

该实验要回答的问题是：

> 即便锚点生成已经升级为 BIC-LR，自适应锚点学习之后，原 3AMVC 的跨视图对齐模块是否仍然必要？

这是当前主线里很关键的一点。因为如果去掉对齐后性能几乎不变，那么说明：

- BIC-LR 的改进已经足以主导结果；
- 原 3AMVC 的对齐模块贡献有限。

反之，如果去掉对齐后性能明显下降，则说明：

- 锚点生成改进并不能替代跨视图对齐；
- 原 3AMVC 的对齐依然是主线不可删除部分。

### 5.2 对照方法

本实验采用三方法对照：

- `M1 Original 3AMVC`
- `M2 BIC-LR-3AMVC`
- `M7 w/o alignment`

其中 `M7 w/o alignment` 的定义为：

- 保留完整 BIC-LR 锚点选择。
- 保留 `lambdaBIC`。
- 保留 `minNodeSize`。
- 保留每个视图的锚点质量评分 `q_v`。
- 保留基准视图选择规则 `b = argmin q_v`。
- 不执行跨视图锚图对齐。
- 不执行跨视图等权融合。

注意：

`w/o alignment` 不能错误实现成“直接把不同视图锚图相加”。

原因是：

- 各视图锚点数通常不同；
- 不对齐时锚图列维度不兼容；
- 直接平均没有数学意义。

因此，本实验最稳妥、最干净的执行方式应定义为：

- 只使用“基准视图”的锚图进入最终谱嵌入与 KMeans。

也就是：

1. 仍然对所有视图做 BIC-LR 锚点生成；
2. 仍然计算所有视图的 `q_v`；
3. 仍然选出基准视图 `b`；
4. 只保留基准视图锚图 `Z^(b)` 做最终聚类；
5. 完全跳过 `aligned.m` 和后续多视图融合。

### 5.3 变量控制

必须统一控制：

- 数据集与预处理一致。
- `beta` 搜索范围一致。
- `lambdaBIC` 搜索范围一致。
- `minNodeSize` 搜索范围一致。
- `randomSeed` 一致。
- KMeans 重复次数一致。
- `tauSplit=0` 固定。
- `epsVar / maxAnchors / verbose` 固定。

这里需要特别说明：

- `M7` 中 `lambda` 已经失效；
- 因为没有对齐项，也就没有跨视图结构一致性正则；
- 所以 `lambda = N/A`，不应继续搜索。

### 5.4 推荐执行步骤

#### 阶段一：粗搜索

1. 对 `M2` 执行完整粗搜索，得到参考 best `ACC` 区域。
2. 对 `M7` 搜索 `beta / lambdaBIC / minNodeSize`。
3. `M1` 作为原始基线，按原方法独立求其 best `ACC`。
4. 每个方法都保留平均 `ACC` 最好的若干组进入精评。

#### 阶段二：精评

1. 提高 KMeans 重复次数。
2. 对 `M7` 的候选组合重复评价，得到平均 `ACC` 与标准差。
3. 用平均 `ACC` 选出 `M7` best 结果。
4. 在 best `ACC` 对应参数下同步输出全部仓库已有指标。

### 5.5 本实验重点观察什么

本实验的关键不是锚点数，而是：

- 去掉对齐以后，`ACC` 是否明显下降；
- `NMI / Purity / Fscore` 是否同步下降；
- 基准视图单独聚类是否足以替代多视图对齐融合；
- 运行时间是否下降；
- 性能下降和效率下降是否形成明确权衡。

换句话说，本实验希望回答：

- 多视图改进后的好结果，到底来自“更好的单视图锚点”，还是来自“更好的多视图协同”。

### 5.6 如何解释可能结果

推荐按以下逻辑解释：

- 若 `M7` 的 best `ACC` 明显低于 `M2`，则可直接支持“alignment 仍然必要”。
- 若 `M7` 耗时明显更低，但 `ACC / NMI / Fscore` 同步下降，则说明：
  - 去掉对齐确实能省时间；
  - 但会破坏多视图协同收益；
  - 对齐模块保留是合理的。
- 若 `M7` 与 `M2` 差距很小，则需要进一步检查：
  - 是否某些数据集本身多视图互补性较弱；
  - 是否 BIC-LR 已经让基准视图足够强；
  - 是否某些视图对齐质量本来不高。

---

## 六、三个主消融实验的统一参数建议

### 6.1 粗搜索

建议沿用当前仓库已有主线量级：

- `MFeat_2Views`
  - `beta = {1, 10, 100}`
  - `lambda = {1e3, 1e4, 1e5}`
  - `lambdaBIC = {0.5, 1, 2}`
  - `minNodeSize = {10, 20, 40}`

- `Reuters-1200`
  - `beta = {1, 10, 100}`
  - `lambda = {1e2, 1e3, 1e4}`
  - `lambdaBIC = {0.5, 1, 2}`
  - `minNodeSize = {8, 16, 32}`

- `Wikifea`
  - `beta = {1, 10, 100}`
  - `lambda = {1e2, 1e3, 1e4}`
  - `lambdaBIC = {0.5, 1, 2, 4}`
  - `minNodeSize = {10, 20, 40}`

- `Caltech101-all`
  - `beta = {10, 100}`
  - `lambda = {1e2, 1e3, 1e4}`
  - `lambdaBIC = {1, 2, 4}`
  - `minNodeSize = {40, 80, 160}`

### 6.2 精评

围绕粗搜索得到的候选 best `ACC` 组合做局部精化：

- `beta`：围绕粗最优点局部扩展。
- `lambda`：围绕粗最优点做近邻倍率扩展。
- `lambdaBIC`：围绕粗最优点做细粒度补点。
- `minNodeSize`：围绕粗最优点做相邻整数段补点。

但要注意：

- `M3` 不搜索 `lambdaBIC`
- `M4` 不搜索 `lambdaBIC`
- `M7` 不搜索 `lambda`

---

## 七、统一结果记录格式

### 7.1 主表

建议主表字段：

`Dataset | Method | ACC | NMI | Purity | Fscore | Precision | Recall | AR | Entropy | Runtime | AnchorNums | BaselineView`

其中：

- `ACC` 为唯一选优字段。
- 其余字段全部在 best `ACC` 参数组合下同步报告。

### 7.2 明细记录

建议为每次运行都保存如下字段：

- `dataset`
- `method`
- `stage`
- `beta`
- `lambda`
- `lambdaBIC`
- `minNodeSize`
- `tauSplit`
- `epsVar`
- `randomSeed`
- `ACC`
- `NMI`
- `Purity`
- `Fscore`
- `Precision`
- `Recall`
- `AR`
- `Entropy`
- `Runtime_total`
- `Runtime_anchor`
- `Runtime_align`
- `targetView`
- `anchorCounts`
- `qualityScores`
- `hasTinyNodes`
- `hasAbnormalAnchors`
- `cacheKey`

### 7.3 选优表

建议再单独汇总一张“选优过程表”：

`Dataset | Method | SearchStage | ActiveParams | MeanACC | StdACC | SelectedForRefine`

这样后续撰写论文时可以清晰回答：

- 这个 best `ACC` 是怎么来的；
- 它是粗搜索筛出来的，还是精评最终确认的。

---

## 八、缓存与复现建议

### 8.1 缓存键

无论哪个主消融实验，锚点缓存键至少应包含：

- `dataset`
- `preprocess`
- `method`
- `lambdaBIC`
- `minNodeSize`
- `randomSeed`

因为：

- 不同方法不能共享锚点缓存；
- 不同 `seed` 不能共享随机结果；
- 不同 `lambdaBIC / minNodeSize` 不能共享锚点。

### 8.2 本轮主表缓存策略

如果主表只重复最终 KMeans，则：

- 同一 `dataset + method + lambdaBIC + minNodeSize + seed`
- 允许直接复用锚点生成结果

但：

- `M2` 与 `M3` 不能共用缓存；
- `M2` 与 `M4` 不能共用缓存；
- `M2` 与 `M7` 不能共用缓存。

原因是：

- 三个方法的分裂准则不同；
- 生成的锚点集合不应视为同一对象。

---

## 九、最终推荐的撰写口径

如果后续要把这三个实验写进论文或报告正文，建议按以下逻辑组织：

1. 先给出 `M1` 与 `M2` 的总对比，说明完整 BIC-LR-3AMVC 相比原始 3AMVC 是否整体更优。
2. 再分别给出三个主方向消融：
   - `w/o BIC`
   - `w/o LR`
   - `w/o alignment`
3. 每个消融都围绕同一个问题展开，不混入其他旧分支。
4. 所有方法都按 best `ACC` 选优，但表格中必须保留仓库现有全部指标。

推荐的结论模板如下：

- `w/o BIC` 说明复杂度惩罚是否必要。
- `w/o LR` 说明统计建模是否必要。
- `w/o alignment` 说明多视图对齐是否必要。

只要这三点结论清楚，当前主方法的“必要组成”就能被交代完整。

---

## 十、当前不应做的事情

本轮主消融方案中，明确不应做以下事情：

1. 不把 `quality_weighted` 或 `quality_alignment_weighted` 纳入主消融。
2. 不把 `rho`、`tauS` 纳入当前主实验。
3. 不把 `tauSplit` 当成主搜索超参数。
4. 不把 `epsVar`、`maxAnchors` 当成主搜索超参数。
5. 不把 `w/o alignment` 错写成“未对齐直接多视图平均”。
6. 不把单次最优 `ACC` 当成最终 best `ACC`。
7. 不在方法之间混用不同的预处理、不同的重复次数、不同的评价协议。

当前最重要的是：

- 三个主消融方向足够清楚；
- 每个实验如何执行足够具体；
- 所有方法都按 best `ACC` 选优；
- 其余仓库已有指标全部保留。

